import io
from PIL import Image

# Save the dendrogram as an image
img_bytes = dendrogram.to_image(format="png")

# Display the dendrogram image in the Streamlit app
st.image(Image.open(io.BytesIO(img_bytes)))
