import numpy as np
import matplotlib.pyplot as plt
from scipy.cluster.hierarchy import dendrogram, linkage

# Assuming you have your linkage matrix in a variable 'linkage_matrix'
# You should have already created the linkage matrix during clustering

# Create a dendrogram
dendrogram(linkage_matrix)

# Add labels and titles
plt.title('Hierarchical Clustering Dendrogram')
plt.xlabel('Sample Index')
plt.ylabel('Distance')

# Show the plot
plt.show()
