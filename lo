# Create a DataFrame for the cluster labels
cluster_df = pd.DataFrame({'Cluster': voting_labels})

# Reset the index of the cluster DataFrame to match the original data
cluster_df.reset_index(drop=True, inplace=True)

# Add the cluster DataFrame to the original data
df = pd.concat([df, cluster_df], axis=1)