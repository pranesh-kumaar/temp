button = ttk.Button(tab1, text = "Submit", style="Accent.TButton", command =  threading.Thread(target=submit_0).start)

def submit_0():
        
        pythoncom.CoInitialize()
        sid = SID_entry.get()
        name = name_entry.get()
        market = Market_entry.get()
        advisor = advisor_entry.get()
        deliverable = dl.get()
        other = other_entry.get()
        client_name = client_entry.get()
        value = value_entry.get()
        activity = dq.get()
        comment = comment_entry.get()

        data = [name, sid, market, advisor, deliverable, other, client_name, value, activity, comment]
        
        fill_form(data, 0)
        threading.Thread(target=submit_0).join
