# Create an interactive tree plot using Plotly
fig = px.treemap(
    names=cluster_labels,
    labels=cluster_labels,
    parents=[f'Cluster {i}' for i in range(n_clusters)],
)

# Display the tree plot in the Streamlit app
st.plotly_chart(fig)
