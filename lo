import pandas as pd

# load the data from a CSV file
data = pd.read_csv('technical_data.csv')

# select the columns you want to use as features
features = ['High', 'Low', 'EMA', 'Stoch', 'RSI']

# create the X matrix
X = data[features].values
